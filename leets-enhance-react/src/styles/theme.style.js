const FONT_SIZE = {
  small: "10px",
  medium: "18px",
  large: "20px",
};

const FONT_WEIGHT = {
  thin: 300,
  regular: 500,
  bold: 800,
};

const theme = {
  FONT_SIZE,
  FONT_WEIGHT,
};

export default theme;
